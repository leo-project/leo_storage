language: erlang
script: "make"
notifications:
  email: false
otp_release:
  - R16B03-1
before_install:
  - sudo apt-get install -y automake autoconf libtool yasm
install:
  - sudo apt-get install -y build-essential cmake check
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 90
